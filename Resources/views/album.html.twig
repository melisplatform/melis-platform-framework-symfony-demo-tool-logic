{% block album_list %}
    <h3>Album List</h3>
    <p>
        {% if translate %}
            {% trans %} tool.album_table_desc {% endtrans %}
        {% else %}
            The table below shows the data fetched from the database using Symfony.<br/>
            <code>$album = $this->getDoctrine()->getRepository(Album::class)->findAll();</code>
        {% endif %}
    </p>

{#    Create table using create_table helper#}
    {{ create_table(
        {
            'id' : 'tableSymfonyDemoTool',
            'class' : 'table table-striped table-primary dt-responsive nowrap',
            'cellspacing' : '0',
            'width' : '100%'
        },
        tableColumns
        )|raw
    }}

{#    Initialize data table to make a pagination#}
    <div>
        <script type="text/javascript">
            $(document).ready(function() {
                {{ getDataTableConfiguration|raw }}
            });
        </script>
    </div>
{% endblock %}