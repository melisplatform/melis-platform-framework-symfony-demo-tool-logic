{% block album_form %}
    {{ form_start(form) }}
    <div class="form-group">
        {{ form_row(form.alb_name) }}
    </div>
    <div class="form-group">
        {{ form_row(form.alb_song_num) }}
    </div>
    {{ form_end(form) }}
{% endblock %}